// Exemplo na classe triangulo. Construir com base nela
#pragma once
#include "bibSistema.h"

/*
class Triangle {
    private:
        float triangleVertex[3][2]; // Armazena os vértices do triângulo
        float triangleColor[3];     // Armazena a cor do triângulo (RGB)
    
    public:
        Triangle(float v0[2], float v1[2], float v2[2], float color[3]);                 // Construtor
        void drawTriangle(void);    // Método para desenhar o triângulo
    };
*/

// class NaveJogador {

//     private:
//         GLfloat posicao[2];
//         GLfloat velocidade;

//         GLvoid corpo(GLvoid);
//         GLvoid cockpit(GLvoid);


// }